# 카페스이 직원 시스템 (ops/index.html) 작업 규칙

## 배포
- 브랜치 `claude/new-session-7x08jn`에 커밋 후 `main`에도 같이 push (GitHub Pages: https://thsalswl900119-maker.github.io/SONJOY/ops/).
- 배포마다 화면의 버전 표시 `v0924-N`을 1 올린다.
- 커밋 메시지는 한국어. 저장소 파일·커밋에 모델 이름을 넣지 않는다.

## 데이터
- 저장된 데이터는 절대 지우거나 덮어쓰지 않는다. 이관·병합은 "빈 칸만 채우기"로만.
- 어떤 기기에서 저장해도 반드시 서버(Firestore `ops` 컬렉션)에 남아야 한다.
- 국기는 항상 🇰🇷. Firebase 비밀번호는 대화에 쓰지 않는다.

## 화면 규칙 (사장님이 "앞으로 이런 식으로" 라고 확정한 스타일)
- **규칙·중요사항 박스**: 빨간 테두리 박스 + 번호 배지(빨간 원) + 각 항목은 흰 카드 한 줄.
  제목은 굵게, 핵심 구절만 노란 형광(`<mark>`, 빨간 글씨), 아래 줄에 설명. 예: `.tnotice` 스타일.
- **할 일 색 체계**: 빨강(`imp big`)=매일 꼭 · 품질/입고 확인, 파랑(`imp blue`)=확인·체크 작업, 초록(`imp green`)=재고·발주 파악, 노랑(요일 태그 `.twd` 있는 줄)=수/금 같은 특정 요일 작업.
- 사장님 지시사항은 항상 굵고 진하게. "있을 때만" 표기는 보고서에 넣지 않는다.
- 긴 문장은 2줄로 보이되 잘리지 않게. 버튼은 탭 크기와 맞춘다.
- 직원 이름에는 "님"을 붙인다. 문구 요청은 준 그대로 반영한다.
- **오늘 할 일 편집**: 직원이 「✏ 업무 편집」으로 문구·순서·색·묶음을 바꾸거나 추가·삭제할 수 있다. 바뀐 내용은 `cafesui.todo.custom`에 저장되어 HTML 위에 덮어씌워지므로, HTML의 항목 키(`data-k`)와 묶음 제목(`.tbhead`)은 함부로 바꾸지 않는다 (바꾸면 직원 편집이 풀릴 수 있음).
- **접속 표시**: `cafesui.presence.<기기번호>`에 누가 어느 화면(일지 날짜·재고 날짜 등)을 쓰는지 20초마다 남긴다. 이 키는 백업·되돌리기·새로고침 알림에서 제외한다(스냅샷은 `cs:presence`만 보냄).
- **속도(휴대폰)**: 글자 칠 때마다 전체를 다시 그리지 말 것(보고서·집계는 250ms 모아서). `:has()` 선택자 금지(요일 줄은 `.hastwd` 클래스). 큰 묶음(`.tblock .msec .skgrp .tdrs .ztable-wrap`)은 `content-visibility:auto`. 문서 전체를 뒤지는 MutationObserver는 모아서(400ms) 한 번만.
- **파일 구조(v0924-90~)**: 프로그램 코드는 `ops/js/*.js`(sync·holidays·sched·app·stock·presence), 자주 안 쓰는 탭 내용은 `ops/tabs/*.html`(처음 열 때 불러옴). `index.html`에는 화면 틀과 스타일만. 배포 전 `python3 ops/bump.py`로 버전을 올리면 js 주소의 `?v=`도 같이 바뀐다(옛 파일 섞임 방지).
- **일지 저장(v0924-95~)**: 저장은 「이 화면에서 바뀐 칸만」 쓴다(base 비교). 안 건드린 칸은 서버 최신값 유지 — 다른 기기 글을 옛 화면으로 덮지 않기. 줄 작성자 `bl`, 사장님이 직원 줄 중간에 끼워 쓴 글은 `ow`(칸별 글 목록)로 따로 기억해 노란 형광·「👑 사장님 덧붙임」.
- **글자색**: 정항아 주황(#C45A00) · 박혜빈 파랑(#1B5FC1) · 이해선 보라(#8E3BB0) · 사장님 초록(#2E7D32). 테마색(--open/--close)과 같은 색 쓰지 말 것(구분 안 됨).
